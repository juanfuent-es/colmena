<% provide(:title, "Editando Administrador") %>

<header class="flex flex-col-reverse md:items-center md:flex-row border-b border-white/5 py-4 sm:py-6 mb-2 md:mb-4">
  <div class="sm:flex-auto">
    <h1 class="leading-6 text-xl font-medium">Editando Administrador</h1>
  </div>
  <nav class="my-4 sm:flex-none flex gap-2">
    <%= link_to admin_users_path, class: 'btn-xs danger' do %>
      <i class="fa-solid fa-arrow-left pr-1"></i> Regresar
    <% end %>
    <%= link_to admin_change_password_path(id: @user.id), class: "btn-xs" do %>
      <i class="fa-solid fa-key pr-1"></i> Recuperar Contraseña
    <% end %>
  </nav>
</header>

<%= render 'form', user: @user %>