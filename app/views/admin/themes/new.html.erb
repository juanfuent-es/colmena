<% provide(:title, "Nueva theme" ) %>

<header class="flex flex-col-reverse md:items-center md:flex-row border-b border-white/5 py-4 sm:py-6 mb-2 md:mb-4">
  <div class="sm:flex-auto">
    <h1 class="leading-6 text-xl font-medium">
      <%= "#{t(:new)} #{t(:theme)}" %>
    </h1>
  </div>
  <nav class="text-sm flex gap-4">
    <%= render "components/link" , href: admin_program_themes_path(@program), text: "Regresar" ,
      icon: "fa-solid fa-arrow-left" %>
  </nav>
</header>


<%= render 'form' , theme: @theme %>