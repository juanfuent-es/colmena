<% provide(:title, "Lista de #{t(:quotations)}") %>

<!-- Content Header (Page header) -->
<header class="content-header">
  <div class="container-fluid">
    <div class="row mb-2">
      <div class="col-sm-12">
        <h1 class="card-title">Lista de <%= t(:quotations) %> <sup><%= Quotation.all.count %></sup></h1>
      </div>
    </div>
  </div><!-- /.container-fluid -->
</header>

<!-- Main content -->
<section class="content">
  <!-- Default box -->
  <div class="card card-primary card-outline">
    <header class="card-header">
      <%= link_to new_admin_quotation_path, class: 'btn btn-outline-primary btn-sm float-right' do %>
        <i class="fas fa-plus"></i> <%= "#{t(:new)} #{t(:quotation)}" %>
      <% end %>
    </header>
    <div class="card-body">
      <table class="table table-hover table-sm datatable text-sm">
        <thead>
          <tr>
            <th>Editar</th>
            <th class='text-center'>Eliminar</th>
          </tr>
        </thead>

        <tbody>
          <% @quotations.each do |quotation| %>
            <td>
            <%= link_to edit_admin_quotation_url(quotation), class: "btn btn-link btn-sm" do %>
              <i class="fas fa-pencil-alt"></i> Editar quotation
            <% end %>
            </td>
            <td class='text-center'>
              <%= link_to admin_quotation_path(quotation), method: :delete, class: "btn btn-outline-danger btn-sm" , data: { confirm: "¿Estás seguro de eliminar este #{t(:quotation)}?" } do %>
                <i class='fas fa-trash'></i> Eliminar
              <% end %>
            </td>
            </tr>
          <% end %>
        </tbody>
      </table>
    </div>
    <!-- /.card-body -->
  </div>
  <!-- /.card -->

</section>
<!-- /.content -->